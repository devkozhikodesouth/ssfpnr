// Shared Tailwind class strings for admin form controls — keeps inputs visually
// consistent across every shared field component without duplicating the strings.
export const inputClass =
  'w-full px-3 py-2 bg-gray-800 border border-gray-700 rounded-lg text-white placeholder-gray-500 focus:outline-none focus:border-emerald-500 focus:ring-1 focus:ring-emerald-500'

export const labelClass = 'block text-sm text-gray-300 mb-1'

export const chipBase = 'px-3 py-1 rounded-full text-sm font-medium transition-colors'
