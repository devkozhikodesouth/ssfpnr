/**
 * STUB — SEO helpers replaced in Phase 8.
 * @returns {Object}
 */
function buildMetadata() {
  return {}
}

/**
 * STUB — JSON-LD builder replaced in Phase 8.
 * @returns {null}
 */
function buildJsonLd() {
  return null
}

module.exports = { buildMetadata, buildJsonLd }
